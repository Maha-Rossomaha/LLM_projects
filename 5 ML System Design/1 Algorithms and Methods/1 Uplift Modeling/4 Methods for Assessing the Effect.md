
## Matching (сопоставление)

**Идея:**  
Найти и сравнить **похожие наблюдения** из treatment ($W=1$) и control ($W=0$) групп.  
Для каждого объекта из одной группы подбираются “близкие” по признакам аналоги из другой группы, и сравниваются их исходы $Y$. Разница внутри таких пар считается приближённой оценкой индивидуального эффекта.


**Как измеряется «похожесть»**

1. **По propensity score $e(X)$** — самый популярный способ: ищутся объекты с близкими значениями вероятности попасть в treatment.  

2. **По Махаланобису** — учитывает корреляцию между признаками, подходит, если признаки не нормированы или имеют разный масштаб.

3. **По выбранным признакам** — если есть небольшой набор ключевых ковариат (например, возраст, доход), можно считать расстояние напрямую по ним.

$\;$  
**Caliper (радиус подбора)**

Чтобы избежать "плохих" пар (слишком разные объекты), задают **максимально допустимую дистанцию** между treatment и control. Если подходящего соседа нет в пределах этого радиуса — объект исключается. Это повышает качество, но может уменьшить объём данных.

$\;$   
**Соотношение пар**

- **1:1 matching** — каждому объекту из treatment подбирается ровно один из control.  
- **1:k matching** — одному объекту из treatment подбирают несколько из control (увеличивает стабильность).  
- **With / without replacement** — можно разрешить одному контролю участвовать в нескольких парах или запрещать повторное использование.

$\;$   
**Типы оценок**

- **ATE (Average Treatment Effect):** симметричное сопоставление обеих групп — оцениваем средний эффект для всей популяции.  

- **ATT (Average Treatment effect on the Treated):** сопоставляем только обработанных (treatment) с похожими из control — оцениваем эффект *для тех, кто реально получил воздействие*.

$\;$   
**Плюсы:**
- Простая и понятная интерпретация ("сравниваем похожих").  
- Уменьшает модельность — часто можно обойтись без сложных моделей.  

**Минусы:**
- Потери данных (если нет подходящих пар).  
- Чувствительность к выбору метрики и радиуса (caliper).  
- При высокой размерности признаков трудно найти хорошие совпадения.

---

## IPTW (взвешивание обратными вероятностями)

**Идея:**  
Сделать так, чтобы treatment ($W=1$) и control ($W=0$) группы имели **одинаковое распределение признаков $X$**,  
как будто воздействие назначалось случайно.  
Для этого каждому наблюдению назначается вес, обратный вероятности попасть в свою группу.

---

### Как это работает

Если вероятность treatment равна $e(X)$, то:
- объект из treatment-группы получает вес $1 / e(X)$,
- объект из control-группы получает вес $1 / (1 - e(X))$.

Интуиция:  
наблюдения, которые **могли попасть в обе группы с примерно равной вероятностью**,  
получают небольшой вес;  
а те, которые оказались “не там, где ожидалось” (например, редкие случаи),  
взвешиваются сильнее, чтобы “восстановить баланс”.

---

### Оценка ATE через IPTW

$$
\widehat{\text{ATE}}_{\text{IPTW}} = \frac{1}{n} \sum_{i=1}^n \Bigg( \frac{W_i Y_i}{\hat e(X_i)} - \frac{(1-W_i) Y_i}{1 - \hat e(X_i)} \Bigg)
$$

Это разность средних откликов в двух "взвешенных" популяциях,  
где после взвешивания распределения $X$ у treatment и control становятся похожими.

---

### Проблема дисперсии и решения

Когда $\hat e(X)$ близко к 0 или 1, веса становятся огромными,  
и оценки сильно колеблются.

Чтобы это исправить:

- **Стабилизация весов:**  
  домножают на вероятность treatment в целом  
  (например, $P(W=1)$ для обработанных и $P(W=0)$ для контролей).  
  Это снижает разброс весов.

- **Trimming / обрезка:**  
  ограничивают $e(X)$ в диапазоне, например [0.05, 0.95],  
  чтобы не допустить бесконечных весов.

---

### Варианты весов

- **ATT (Average Treatment Effect on the Treated):**  
  фокус на обработанных; контрольная группа получает меньшие веса.  

- **ATC (на контролях):**  
  аналогично, но фокус на контрольной группе.  

- **Overlap-веса:**  
  дают больший вес наблюдениям, у которых $e(X)$ ближе к 0.5 —  
  то есть в области, где есть перекрытие между группами.  
  Это делает оценку устойчивее и ближе к реальной зоне сравнения.


**Плюсы:**
- Простая реализация — только модель $e(X)$ и формула для весов.  
- Можно использовать для ATE, ATT и других метрик.  

**Минусы:**
- Чувствительность к экстремальным вероятностям $e(X)$.  
- Нужна аккуратная калибровка и ограничение весов.

📘 **Итог:**  
IPTW создаёт "виртуальную популяцию", где treatment и control сбалансированы по признакам,  
что позволяет корректно оценить причинный эффект даже в наблюдательных данных.

---

## 9) Двойная робастность (предвосхищение)

Хотя подробно это в другом конспекте, зафиксируем идею: соединить **модель исхода** $m_w(x)=\mathbb{E}[Y\mid X=x,W=w]$ и **модель пропенсити** $e(x)$ так, чтобы при корректности хотя бы одной из них оценка ATE была состоятельной.

**AIPW/DR‑оценка ATE:**

$$
\widehat{\text{ATE}}_{\text{DR}} = \frac{1}{n}\sum_{i=1}^n \Bigg( \big(\hat m_1(X_i)-\hat m_0(X_i)\big) + \frac{W_i}{\hat e(X_i)}\big(Y_i-\hat m_1(X_i)\big) - \frac{1-W_i}{1-\hat e(X_i)}\big(Y_i-\hat m_0(X_i)\big) \Bigg).
$$

Это базис для **R‑/DR‑/X‑learners** и **Double Machine Learning** в задачах CATE/uplift.

---