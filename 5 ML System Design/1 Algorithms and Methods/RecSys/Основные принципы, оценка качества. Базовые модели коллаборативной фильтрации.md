---
Owner: IIvan Vashchenko
---
> [!important] **
> Основные принципы рекомендательных систем, оценка качества и постановка эксперимента. Базовые модели на основе коллаборативной фильтрации.

# **Рекомендуемая литература**

_**Main book:**_

- Personalized Machine Learning, Julian McAuley, 2022 (in press);  
      
    [https://cseweb.ucsd.edu/~jmcauley/pml/pml_book.pdf](https://cseweb.ucsd.edu/~jmcauley/pml/pml_book.pdf) (база, начало мльное)

_**Additional reading (in no particular order):**_

- **Recommender Systems. The Textbook**, 2016; Charu C. Aggarwa (задачки - устаревше)
- **Recommender Systems: An Introduction**, 2010; D. Jannach, M. Zanker, A. Felfernig, G. Friedrich (концептуально актуальности не теряет)
- **Mining Massive Datasets**, Stanford University, [http://www.mmds.org](http://www.mmds.org/)
- **Collaborative Recommendations: Algorithms, Practical Challenges and Applications**; S. Berkovsky, I. Cantador and D. Tikk; 2019.
- **Recommender Systems Handbook**, 2022, 3rd edition; F. Ricci, L. Rokach, B. Shapira
- **Statistical Methods for Recommender Systems**, 2016; Deepak K. Agarwal, Bee-Chung Chen
- **Programming Collective Intelligence**, 2007; Toby Segaran

![[image 13.png|image 13.png]]

- **polara** - проект автора
- **RePlay** - сберовская история
- **Recbole** - целый фреймворк
- **implicit** - дефолтная библиотека для построения быстрого решения
- **rs_datasets** - для обработки датасетов
- **recmetrics** - для анализа качества рексистем

  

# Введение

Главная цель - научиться предсказывать пользовательские предпочтения на основе действий из прошлого.

![[image 1 6.png|image 1 6.png]]

  

![[image 2 5.png|image 2 5.png]]

1. При ранжировании товаров в порядке убывания меры релевантности нам хотелось бы учитывать как можно больше сигналов/фичей (как пользователя, так и объекта);
2. Рекомендательная система должна работать достаточно быстро;
3. Должен быть несложный механизм, позволяющий понятно учитывать «бизнес-логику» (например, если при прочих равных мы больше хотим показывать свежие статьи).

# Методы оценки

1. **Offline Evaluation**:
    
    Подходит для начальной оценки, не требуя участия реальных пользователей.
    
    Позволяет оценить способность моделей к обобщению.
    
    Результаты могут не соответствовать бизнес-метрикам или итогам онлайн-тестирования.
    
2. **Online Evaluation**:
    
    Оценка с участием реальных пользователей, применяемая в коммерческих рекомендательных системах.
    
    Напоминает клинические испытания — проводится в контролируемой среде.
    
    Недоступна широкой аудитории, ограничена конкретной областью и набором данных.
    
3. **User Studies (фокус группы)**:
    
    Предполагает привлечение пользователей для решения конкретных оценочных задач.
    
    Возможна предвзятость в зависимости от методологии и выбранной аудитории - есть риск смещенных оценок.
    
    Аудиторию просят оценить качество работы например двух алгоритмов в сравнении.
    
      
    

# **Факторы, влияющие на качество рекомендаций**

1. **Длина профиля пользователя**:
    
    Важно различать активных пользователей и ботов.
    
    Также учитывается тип пользователей — “warm” (иногда активные) и “hot” (очень активные).
    
2. **Концентрация популярных элементов в истории пользователя**:
    
    Сравнивается количество популярных элементов (“short head items”) и нишевых элементов (“long tail items”) в истории пользователя.
    
    Если алгоритм игнорирует популярные или непопулярные элементы, они могут никогда не появиться в рекомендациях, что ограничивает разнообразие предложений.
    
3. **p-core filtering**:
    
    Фильтрация на основе минимального количества элементов для каждой категории, чтобы повысить качество рекомендаций и избежать шумных данных. Например удалить те users и items, у кого меньше $p$ соединений.
    
    Автор называет это одним из способов предобработки датасета. При одном filtering может лучше работать один алгоритм, а при другом - другой, также как и без фильтрации - все истории разные. Вот например пример результатов статьи: описание с habr
    
    ![[image 3 5.png|image 3 5.png]]
    
      
    

  

# Offline evaluation methodologies

![[image 4 5.png|image 4 5.png]]

## Схемы разбиения данных

1. **Classification/Regression or Recommendation?**
    
    Генерация рекомендаций рассматривается как отдельная задача, но её можно сформулировать как задачу классификации или регрессии.
    
    В отличие от задач классификации, в рекомендательных системах сложно работать с новыми пользователями или элементами, что требует особого подхода.
    
    Есть различные подходы к разбиению данных: классическое разбиение для классификации и разбиение по сущностям (entry-wise) для рекомендаций, где нет строгого разделения между зависимыми и независимыми переменными.
    

![[image 5 5.png|image 5 5.png]]

1. **User-wise data split schemes**

![[image 6 5.png|image 6 5.png]]

Речь идет о различных способах разделения данных для оценки рекомендательных систем, чтобы проверить их способность обобщать на новых пользователей или элементы.

  

1. **Оригинальные данные**: Представлены все пользователи и их взаимодействия (например, оценки фильмов, клики и т.д.).
2. **Типы разбиений данных**:
    
    **Если тестовый набор данных полностью не наблюдается** (все взаимодействия для тестовых пользователей убраны из обучающего набора):
    
    Это называется **“warm-start” сценарием** — то есть система обучается на пользователях, которые уже взаимодействовали с системой, и тестируется на новых данных для этих же пользователей.
    
    **Strong generailzation test** — тестирует способность системы обобщать на пользователей с другими предпочтениями и на новые элементы.
    
    **Если тестовый набор данных является частью обучения** (некоторые взаимодействия тестовых пользователей включены в обучающий набор):
    
    В этом случае тестируется способность модели обобщать, учитывая, что она уже видела часть данных о тестовых пользователях.
    
    **Standard scenario**: тестовые пользователи присутствуют в обучении, и модель имеет некоторую информацию о них.
    
    **Weak generalization test**: модель может предсказывать лучше, так как видела часть данных тестовых пользователей.
    
3. **Holdout-тест**:
    
    Данные, которые всегда остаются ненаблюдаемыми для модели, то есть ни одна информация об этих пользователях или взаимодействиях не используется в обучении.
    
    Используется для проверки модели на полном каталоге элементов, чтобы оценить её качество в условиях максимального обобщения.
    
    Существуют разные подходы к тестированию рекомендательной системы, каждый из которых предназначен для оценки её обобщающих способностей в разных условиях: для новых пользователей или новых элементов, либо для тестирования на уже известных пользователях с новыми предпочтениями.
    

  

Подходы к выборке элементов для holdout-теста в рекомендательных системах:

1. **Стратегии**:
    
    **Random**: случайный выбор элементов для holdout-теста.
    
    **Rating-based**: выбор элементов на основе рейтинга (например, самых высоко оцененных).
    
    **Temporal**: выбор последних по времени элементов (например, самых новых взаимодействий).
    
2. **Размер выборки**:
    
    **Fixed number of items**: фиксированное количество элементов (например, 1 элемент на пользователя).
    
    **Fixed percentage of items**: фиксированный процент от общего количества элементов.
    
3. **Типичные стратегии разбиения данных**:
    
    **Random holdout split**: случайное разбиение, например, leave-%-out или leave-m-out (где m — количество элементов, которые исключаются), или использование k-fold кросс-валидации.
    
    **Temporal split**: разбиение по времени, например, оставляя последние взаимодействия или с использованием последовательной оценки (successive evaluation).
    

  

По подходу к оценкам важно помнить про два пункта:

- Не забывай про лик из будущего (temporal splits - successive evaluation)
- Оценивать качество ранжирования не обязательно среди всех миллионов товаров - можно насэмплировать для каждого пользователя товары без interaction, и оценивать метрику с ними (включая реальные interactions).
    
    ![[image 7 4.png|image 7 4.png]]
    
    Однако доказано, что такой подход ведет к смещенным оценкам:
    
    ![[image 8 3.png|image 8 3.png]]
    
    Поэтому нужно понимать, что это действительно может помочь в скорости, но по-возможности, если товаров не много - стоит отказаться.
    
      
    

Неконсистентость константы в target-sampling ведет к невозможжности сравнивать Recall двух моделей в из разных статей (так как чем меньше нулей, тем проще не ошибиться).

![[image 9 2.png|image 9 2.png]]

Эта таблица показывает отсутствие единых стандартов в методах разбиения данных при оценке рекомендательных систем, что создает **методологический хаос**. Разные подходы к разбиению данных приводят к разным результатам, что затрудняет объективное сравнение моделей и понимание их реального качества.

Лектор посоветовал почитать концептуальную статью **[A Troubling Analysis of Reproducibility and Progress in Recommender Systems Research](https://arxiv.org/abs/1911.07698)**

  

## Метрики для оценки качества рекомендаций (Ranking-based)

Не имеет смысла рассматривать классические метрики - все должно быть завязано под top-n

### **nDCG (normalized Discounted Cumulative Gain)**

$nDCG$ оценивает качество ранжирования, учитывая позицию релевантных элементов. Дисконтирование означает, что релевантные элементы, появляющиеся ближе к началу списка, более значимы.

Идея $nDCG$ в том, чтобы не просто оценивать, какие элементы оказались в рекомендациях, но и учитывать их порядок. Более релевантные результаты, появляющиеся выше в списке, приносят системе больший “вес”, чем те же релевантные результаты, расположенные ниже. Таким образом, $nDCG$ мотивирует алгоритм стараться не только выдать правильные ответы, но и расположить их как можно ближе к началу ранжированного списка.

Формула для $DCG$ (Discounted Cumulative Gain):

$$\text{DCG}@n = \frac{1}{|U_{\text{test}}|} \sum_{u \in U_{\text{test}}} \sum_{i \in R_u} \frac{2^{r_{u i}} - 1}{\log_2(\text{rank}(u,i) + 1)}$$

где $r_{u i}$ — истинная оценка рекомендованного элемента, и предполагается $r_{u i}=0$, если элемент не оценен.

Для нормализации $DCG$ мы рассчитываем $iDCG$ (ideal DCG), который является максимальным возможным $DCG$ для заданного списка (идеальное ранжирование). Тогда:  
  

$$nDCG = {DCG \over iDCG}$$

Логарифм нужен для “сглаживания” - чем дальше идем в конец отранжированного списка, тем менее важно нам смотреть туда.

Есть альтернатива (линейная аппроксимация):

$$DCG@n = \frac{1}{|U_{\text{test}}|} \sum_{u \in U_{\text{test}}} \sum_{i \in R_u} \frac{r_{u i}}{\log_2(\text{rank}(u,i) + 1)}$$

Использование $2^{rel} − 1$ или просто $rel$ в числителе $DCG$ зависит от того, как система оценивает "степень релевантности". Когда у нас есть не только бинарная релевантность (релевант/нерелевант), но и рейтинги по шкале (например, от 0 до 3 или от 0 до 5), функция $2^{rel}−1$ экспоненциально увеличивает вес более высоких уровней релевантности. Это значит, что документ с релевантностью 3 будет цениться существенно выше, чем документ с релевантностью 1, а не просто в три раза выше. Таким образом, более релевантные результаты получают диспропорционально больший вклад в метрику, усиливая фокус на выдаче максимально полезных результатов.

В противоположность этому, если система использует простую бинарную оценку (0 или 1), или если важен линейный рост значимости с увеличением релевантности, можно использовать простой $rel$, чтобы не усложнять шкалирование. В таком случае релевантный элемент (1) всего лишь вдвое важнее нерелевантного (0), без дополнительного экспоненциального эффекта.

### **MAP (Mean Average Precision)**

$MAP$ измеряет среднюю точность для всех пользователей или запросов.

$MAP$ смотрит, насколько хорошо система в целом ранжирует релевантные результаты среди нерелевантных по множеству запросов или сценариев. Идея в том, что мы усредняем качество позиционирования релевантных элементов (через показатель “precision” на каждой релевантной позиции) по всем запросам. Это дает интегральную оценку стабильности и точности системы.

Формула для $Precision@K$ (точности на каждом уровне $K$):  
  

$$\text{Precision@K} = \frac{\text{Количество релевантных элементов в топ-} K}{K}$$

Затем средняя точность для одного пользователя рассчитывается как:  
  

$$\small  
\text{AP} = \frac{1}{\text{Количество релевантных элементов}} \sum_{k=1}^N (\text{Precision@K} \times \text{релевантность на позиции } K)$$

$$\text{релевантность на позиции } K =  
\begin{cases}  
1, & \text{если элемент на позиции } K \text{ релевантен} \\  
0, & \text{если элемент не релевантен}  
\end{cases}$$

MAP для всех пользователей (усреднение по всем AP) будет:  
  

$$MAP = \frac{1}{|U|} \sum_{u \in U} AP_u$$

где $|U|$ — общее количество пользователей.

![[image 10 2.png|image 10 2.png]]

  

### **MRR (Mean Reciprocal Rank)**

MRR оценивает позицию первого релевантного элемента в ранжированном списке. Чем выше позиция, тем лучше.

MRR фокусируется на том, насколько быстро в списке результатов мы встречаем хотя бы один релевантный объект. Идея: чем ближе первый релевантный результат к началу, тем лучше. Таким образом, если система постоянно выдает релевантные ответы на самых первых позициях, у нее будет высокий MRR, указывая на хорошую оперативную точность.

Формула для одного запроса или пользователя:  
  

$$\text{RR} = \frac{1}{\text{Ранг первого релевантного элемента}}$$

Средний обратный ранг для всех пользователей будет:  
  

$$MRR = \frac{1}{|U|} \sum_{u \in U} \frac{1}{\text{Ранг первого релевантного элемента для пользователя } u}$$

### **ATOP (Area under the TOPK-curve)**

ATOP измеряет качество рекомендаций среди топ-K элементов.

ATOP можно представить как усреднение качества рекомендации по ряду “срезов” на разных уровнях глубины. Идея в том, чтобы понять, насколько хорошо система справляется при анализе первых нескольких рекомендаций, потом первых нескольких десятков, и так далее. Это позволяет оценить алгоритм не только для топ-1 или топ-10 результатов, но и посмотреть, как поведение изменяется при рассмотрении более длинных списков.

Формула ATOP обычно представляется как площадь под кривой для топ-K:  
  

$$\text{ATOP@K} = \frac{1}{K} \sum_{i=1}^{K} \text{релевантность элемента на позиции } i$$

Здесь K — количество топ элементов, по которым рассчитывается метрика.

_Метрический_ хаос тоже имеет место:

![[image 11 2.png|image 11 2.png]]

## Важные аспекты рекомендательных систем

Приведённые формулы — лишь примеры. На практике метрики усложняются, нормируются по-разному, учитывают веса, параметры и дополнительные данные.

- $U$ — множество всех пользователей системы.
- $I$ — множество всех доступных элементов (объектов, товаров, фильмов и т.п.).
- $R_u$ — множество (или упорядоченный список) элементов, рекомендованных пользователю u.
    
    uu
    
- $∣R_u∣$ — количество рекомендованных элементов для пользователя u.
- $|I|$ — общее количество уникальных элементов в системе.
- $rel(u,i)$ — индикатор релевантности элемента i для пользователя u, например:  
    $rel(u, i) = \begin{cases} 1, & \text{если элемент $i$ релевантен пользователю $u$} \\ 0, & \text{иначе} \end{cases}$  
    Релевантность может быть определена на основе явного (рейтинги, клики, покупки) или неявного (время просмотра, пролистывания) фидбэка.  
    
- $p(i)$ — частота встречаемости или популярность элемента $i$ среди всех пользователей (например, доля пользователей, которые взаимодействовали с $i$).
- $sim(i,j)$ — мера сходства между элементами $i$ и $j$, нормированная от 0 до 1 (0 — полностью несхожи, 1 — идентичны).

  

### **Coverage (Охват)**

_Охват_ — это степень или полнота, с которой система охватывает доступное пространство контента или вариантов. В случае рекомендательных систем это может отражать, насколько разнообразен набор рекомендуемых элементов по отношению ко всему доступному контенту.

$\large\text{Coverage} = \frac{\left|\bigcup_{u \in U} R_u\right|}{|I|}$

Пример: Если у нас есть каталог из 10 000 книг, а алгоритм рекомендаций регулярно предлагает пользователям в сумме только порядка 100 различных наименований, охват будет низким. Высокий охват подразумевает, что система потенциально дает возможность разным пользователям обнаружить как можно более широкую часть каталога.

  

### **Novelty (Новизна)**

_Новизна_ означает, насколько новые или неожиданные элементы предлагает система. Речь идет о том, насколько контент является неизвестным или нетривиальным для конкретного пользователя. Высокая новизна подразумевает, что пользователь получает не только уже знакомый ему контент, но и что-то принципиально новое.

$\large\text{Novelty}(R_u) = \frac{1}{|R_u|} \sum_{i \in R_u} -\log_2(p(i))$

Чем меньше $p(i)$, тем больше будет значение $−log⁡2(p(i))$, и тем новее считается элемент.

Пример: Если пользователь обычно слушает только классику, а система периодически предлагает ему попробовать современный джаз или этническую музыку, это повышает новизну рекомендаций.

  

### **Serendipity (Синергия неожиданности)**

_Serendipity_ — это понятие, близкое к новизне, но более тонкое по смыслу: оно отражает неожиданные, но одновременно и приятно удивляющие рекомендации. Serendipity можно понимать как позитивную «случайную находку»: пользователь не только видит новый для себя контент, но и неожиданно получает удовольствие от чего-то, что сам вряд ли бы стал искать.

Допустим, определим неожиданность $Unexpectedness(u,i)$ как 1 минус максимальное сходство рекомендуемого элемента $i$ с наборами элементов, с которыми пользователь уже знаком. Тогда:

$\large\text{Serendipity}(R_u) = \frac{1}{|R_u|} \sum_{i \in R_u} rel(u, i) \cdot \text{Unexpectedness}(u,i)$

где

$\large\text{Unexpectedness}(u,i) = 1 - \max_{j \in \text{Known}(u)} sim(i,j)$

$Known(u)$ — множество элементов, с которыми пользователь 𝑢 u уже знаком (купил, смотрел, лайкнул).

Пример: Пользователь интересуется научной фантастикой, но система рекомендует ему книгу в жанре магического реализма, о которой он раньше не знал, и она оказывается настолько интересной, что расширяет его кругозор.

А вот как считается в библиотеке [rectools](https://rectools.readthedocs.io/en/latest/api/rectools.metrics.serendipity.Serendipity.html):

![[image 12 2.png|image 12 2.png]]

  

### **Diversity (Разнообразие)**

_Разнообразие_ касается широты спектра рекомендуемых элементов. Если у нас есть 10 рекомендуемых элементов, и все они крайне похожи между собой (например, 10 практически идентичных поп-песен), то разнообразие низкое. Если же среди рекомендаций есть товары или контент из разных категорий, стилей, жанров, авторов, направлений, то разнообразие высокое. Высокое разнообразие полезно, когда цель – удовлетворить разные интересы пользователя или помочь ему расширить горизонт восприятия.

Разнообразие обычно измеряется как средняя “несхожесть” пар элементов из рекомендованного набора.

$\large\text{Diversity}(R_u) = \frac{1}{|R_u|(|R_u|-1)} \sum_{i \in R_u}\sum_{\substack{j \in R_u \\ j \neq i}} (1 - sim(i,j))$

Пример: Видеосервис, предлагающий не только популярные блокбастеры, но и документальные фильмы, независимое кино, сериалы на разных языках и короткометражки.

  

### **Trust (Доверие)**

_Доверие_ отражает степень, в которой пользователь склонен полагаться на рекомендации системы. Если пользователь часто находит предложенный контент полезным, релевантным и интересным, то уровень его доверия к системе будет расти. Низкий уровень доверия, напротив, означает, что пользователь не верит в способность системы понять его предпочтения или дать что-то ценное, и может игнорировать или обходить стороной рекомендации.

Доверие может быть сложно формализовать напрямую, так как оно относится к восприятию пользователя. Однако упростим задачу, предполагая, что доверие пользователя к системе растёт, если рекомендации часто оказываются релевантными. Один из простых вариантов:

$\large\text{Trust}(u) = \frac{\sum_{i \in R_u} rel(u,i)}{|R_u|}$

Системная метрика доверия:

$\large\text{Trust}_{\text{system}} = \frac{\sum_{u \in U} \sum_{i \in R_u} rel(u,i)}{\sum_{u \in U} |R_u|}$

Пример: Если пользователь неоднократно убеждается в точности рекомендаций, он будет более охотно использовать подсказки системы, доверяя, что она «знает» его вкусы.

  

### **Utility (Утилитарность, Полезность)**

_Утилитарность_ или полезность — это оценка реальной практической ценности, которую пользователь извлекает из рекомендуемого контента. Полезность часто связана с тем, достиг ли пользователь своей цели с помощью предложения системы: смог ли он найти нужную информацию, купить подходящий товар, узнать что-то новое, упростить свою задачу.

Полезность можно интерпретировать как среднюю (или суммарную) выгоду, получаемую пользователем от рекомендованных элементов. Полезность может быть определена по-разному: через явный рейтинг удовлетворённости, экономию времени, экономию денег и т.д.

$\large\text{Utility}(R_u) = \frac{\sum_{i \in R_u} b(u,i)}{|R_u|}$

Для всей системы:

$\large\text{Utility}_{\text{system}} = \frac{\sum_{u \in U} \sum_{i \in R_u} b(u,i)}{\sum_{u \in U} |R_u|}$

Если нам неизвестна индивидуальная функция полезности, можно использовать, например, релевантность как приближение: $b(u,i) = rel(u,i)$

Пример: Рекомендации по выбору бытовой техники, которые помогают быстро и точно подобрать оптимальный по цене и характеристикам вариант, обладают высокой утилитарностью.

  

# Коллаборативная фильтрация

![[image 13 2.png|image 13 2.png]]

Наша область интереса - memory-based.

Наша задача, построить следующую модель (каждой паре свою меру “релевантности”). “Customers who like .. also like …”

$f_u = User \times Items \rightarrow Relevance$

  

## Pure item-to-item

Введем просто счетчик совпадений items:

$$\large score_{i2i}(u,i) = \sum_{\substack{j \in I_u}\substack{j \neq i}} pairCount(i,j)$$

$A \in R^{M \times N}$ - матрица взаимодействия пользователь-товар, $M$ - num of users, $N$ - num of items.

$C \in R^{N \times N}$ - матрица “похожести” (co-occurence)

$С = A^T A - diag(diag(A^TA))$

Пусть $p \in B^N$ (бинарный - взаимодействия пользователя по items) - вектор user preferences, значит $r = Cp$ - вектор предсказанных скоров релевантности

$Recommendations = toprec(n) := argmax_{j}^{n}(r_j)$

  

Сам подход i2i очень эффективен и на разреженных матрицах дает хороший бейзлайн. Есть известная проблема - высокая подверженность популярным items. Плюс большой порядок количества items влечет большую размерность матрицы $C$, хоть она и разреженная. По storage можно изловчиться, а compute сложность $O(MN^2)$ . Так как подсчет количества взаимодействия это $O(1)$ в случае разреженности, тогда выходит $O(MN)$

  

## Виды i2i

Вообще существует набор вариантов Item-to-Item рекомендаций, то есть подходов, используемых для рекомендации одного item на основе связей с другим. Рассмотрим основные аспекты, описанные на слайде:

### **1. Pair count strategies:**

Эти стратегии определяют, как формируются связи между элементами:

- **Symmetric / Asymmetric:**
    
    Симметричные пары предполагают, что связь между элементами взаимная (например, если A связано с B, то B связано с A). Асимметричные пары учитывают направление взаимодействия (например, если пользователь сначала смотрел A, а затем B, это не то же самое, что сначала B, затем A).
    
- **Next-only / List-wise:**
    
    В "Next-only" учитываются только непосредственные последовательные действия (например, пользователь сначала смотрит телевизор, а затем — приставку). В "List-wise" анализируется весь список взаимодействий (например, вся история просмотров пользователя) для выявления более сложных паттернов.
    
- **By type / category:**
    
    Стратегии, при которых пары формируются только между элементами одного типа (например, телевизоры с приставками) или только между элементами в одной категории (например, телевизоры с телевизорами).
    

### **2. Value processing:**

Этот раздел посвящен тому, как обрабатываются значения и веса взаимодействий между элементами.

- **Thresholding:**
    
    Устанавливается минимальный порог для учета взаимодействия. Например, пользователь должен посмотреть товар хотя бы 2 раза, чтобы связь была зафиксирована.
    
- **Weighting:**
    
    Присваивание весов взаимодействиям в зависимости от их значимости. Например, покупка товара может быть более весомым действием, чем просмотр.
    
- **Smoothing:**
    
    Применяется для избежания проблем со слишком низкими частотами или шумом. Например, добавление небольшого веса даже редким взаимодействиям, чтобы они могли учитываться в модели.
    
      
    

Для построения Item-to-Item рекомендаций важно учитывать как способ формирования пар элементов (их связи и взаимодействия), так и обработку значений (весов), чтобы улучшить качество рекомендаций и адаптировать их под задачи.

  

## Cosine similarity

Amazon разработал **Item-to-Item Collaborative Filtering** для своих рекомендаций, что стало одной из наиболее известных и цитируемых систем. Основная идея состоит в вычислении схожести между товарами на основе историй покупок пользователей. Это подход, который фокусируется не на пользователях, а на отношениях между товарами.

### **Итеративный алгоритм:**

1. Для каждого товара $I_1$ в каталоге:
    - Определяем всех покупателей, которые купили этот товар ($C$).
2. Для каждого такого покупателя:
    - Анализируем все остальные товары $I_2$, которые он купил.
    - Фиксируем, что покупатель $C$ купил как $I_1$, так и $I_2$, чтобы учитывать это при построении связи между товарами.
3. Для каждой пары товаров ($I_1$,$I_2$):
    - Вычисляем схожесть $\text{sim}(I_1,I_2),$ что определяет, насколько товары похожи друг на друга с точки зрения покупательских паттернов.

### **Метрика схожести (similarity metric):**

Система использует **косинусную меру схожести**, которая определяет угол между "векторами" товаров. Вектор каждого товара представлен как one-hot encoding, где:

- $a_{uk}=1$, если пользователь $u$ купил товар $k$, и 0 иначе.

Формула для косинусной меры выглядит так:

$$\text{sim}(I_1, I_2) = \cos(a_1, a_2) = {{a_1^Ta_2} \over {\lVert a_1 \rVert \lVert a_2 \rVert}}$$

- $a_1^Ta_2$ - скалярное произведение двух векторов, что фактически равняется количеству пользователей, купивших оба товара.
- $\lVert a_1 \rVert,\lVert a_2 \rVert$ - длины (нормы) векторов, определяющие общее количество пользователей, купивших каждый из товаров.

**Пример:**  
Если товар A был куплен пользователями [$U_1$, $U_2$, $U_3$], а товар $B$ — [$U_2$, $U_3$], то их вектора будут:

- $a_A=[1,1,1,0,0]$
- $a_B=[0,1,1,0,0]$
- Косинусное сходство: $\text{sim}(A,B)={{1⋅0+1⋅1+1⋅1} \over \sqrt{3} \sqrt{2}} = {2 \over \sqrt{6}}$.

  

## **Scalability Trick - Incremental Updates in Binary Case**

Одним из вызовов при внедрении рекомендаций на практике является их масштабируемость. Для больших систем, таких как Amazon, число пользователей и товаров настолько велико, что пересчет всех метрик при каждом обновлении данных становится невозможным. Чтобы решить эту проблему, используется **инкрементальное обновление метрик**.

Основные элементы:

1. **Косинусная мера через подсчет пар:**  
    Косинусная схожесть между товарами $i$ и $j$ может быть выражена через:
    
    $\large sim(i,j)={{\text{pairCount}(i,j)} \over {\sqrt{itemCount(i)⋅itemCount(j)}}}, i \neq j$
    
    Где:
    
    - $\text{pairCount}(i, j) = \sum_u a_{ui} a_{uj}$: Количество пользователей, купивших оба товара.
    - $\text{itemCount}(i) = \sum_u a_{ui}$: Общее количество пользователей, купивших товар $i$.
2. **Обновление при добавлении данных:**  
    При добавлении новых взаимодействий $\Delta A$ (например, новые покупки пользователей), вместо полного пересчета матрицы, можно обновить значения следующим образом:
    - Новая матрица взаимодействий:  
        $A' = A + \Delta A$
    - Новая косинусная схожесть:
        
        $\Large\text{sim}'(i, j) = \frac{\text{pairCount}(i, j) + \sum_u [\Delta A]_{ui} [\Delta A]_{uj}}{\sqrt{\text{itemCount}(i) + \sum_u [\Delta A]_{ui}} \cdot \sqrt{\text{itemCount}(j) + \sum_u [\Delta A]_{uj}}}$
        

### **Зачем это нужно:**

Эта техника позволяет быстро обновлять схожесть между товарами, избегая повторного пересчета всей матрицы. Это критично для больших систем, где данные обновляются в реальном времени

### **Основные идеи и выводы:**

- **Item-to-Item подход** в Amazon стал стандартом для рекомендаций благодаря своей простоте и эффективности.
- Косинусная мера схожести позволяет точно определять отношения между товарами на основе пользовательских данных.
- **Инкрементальные обновления** — важный инструмент для масштабируемости, позволяющий эффективно работать с динамически обновляемыми данными в больших системах.

  

Покрытие по пользователям (при отсутствии важных вводных инструкций) базово важнее, чем покрытие по товарам (например идея такая, что собираем клики).

Стоит помнить про нишевые но релевантные товары (чтобы не возникал “эффект Гарри Поттера”). Рассмотрим на примере кейса Озон

![[image 14.png]]

Их главный трюк заключался в следующем: рассматривать не просто попарные счетчики, а учитывать также частоту встречаемости товаров друг без друга. Таким образом счетчики естественным образом взвешивались на обратную частоту “несовстречаемости”.